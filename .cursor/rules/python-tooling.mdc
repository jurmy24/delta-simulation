---
description: Python package management and tooling conventions
alwaysApply: true
---

# Python Tooling

- This project uses **uv** as the Python package manager.
- Use `uv add <package>` to add dependencies (not pip install).
- Use `uv run <command>` to run scripts in the project environment.
- The virtual environment is at `.venv/` managed by uv.
